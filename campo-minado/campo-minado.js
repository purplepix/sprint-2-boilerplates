
* x * *
* * x *
* * * *
x x * *

var bombs = [[1, 0], [2, 1], [0, 3], [1, 3]];

//localizando as minas e transformando elas em *

function placeBombs(array) {

  }
}


//perguntar o tamanho do campo e criar um campo (array) com esse tamanho com um field. e clicar ok.
  //Dá pra perguntar quantas bombas a pessoa quer colocar por "fácil", "médio", ou "difícil".

//Ao clicar ok, ligar a função pra criar o campo (cria uma array com as coordenadas) e a
//função pra gerar bombas randômicas usando os parâmetros estabelecedidos no começo.
  //Fazer um for dentro de um for e passar por todos os elementos da futura tabela. Se a combinação de (i, j) === bomba[1] (=== (i, j),
  //inserir um asterisco nessa array no lugar desse par. Se não tiver, inserir 0 no lugar.
    //Verificar se o número tá numa array com mines.include(0, 7);
    //transformar o valor da array em string (stringfy) pois o valor que entra em tabela é string.

//Desenhando a tabela
  //Criar campo na tabela: .createElement
  //Escrever no campo da tabela: .textContent os valores 0 ou *
  //Conectar o texto ao campo: .appendChild
  //Adicionando: for dentro de for

//Deixando os valores da tabela em branco: colocar o número dentro de um span e, no css, colocar visibility:hidden

//Fazendo o jogo funcionar:
  //Criar um evento click: se for bomba, acabou o jogo, se não for, só desabilitar o hidden
  //event.target --> mostra onde ele clicou.
  //setAttribute('class', '') ou ('class', 'visible'), algo assim.

//Quando perder, mostrar tudo. E reiniciar o jogo ou ter um botão para reiniciar o jogo.

//Firulas: criar um evento com o botão direito para colocar bandeirinha onde acha que tem bomba.
  //Metakey(tecla comand no mac pra selecionar o botão esquerdo), poderia ser doubleclique
